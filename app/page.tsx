// A lista de conversas mora em components/ChatList.tsx, montada uma única
// vez pelo ResponsiveShell (app/layout.tsx): no celular ocupa a tela cheia
// quando a rota é "/"; no desktop fica fixa como coluna da esquerda. Esta
// página não precisa renderizar nada — o shell decide o que aparece aqui.
export default function HomePage() {
  return null;
}
